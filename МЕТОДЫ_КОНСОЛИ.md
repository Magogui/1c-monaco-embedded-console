# Методы консоли BSL - Полный справочник

## 📋 Содержание
1. [Управление отображением](#управление-отображением)
2. [Работа с текстом](#работа-с-текстом)
3. [Навигация и поиск](#навигация-и-поиск)
4. [Закладки](#закладки)
5. [Координаты и выделение](#координаты-и-выделение)
6. [Темы оформления](#темы-оформления)
7. [Маркеры и ошибки](#маркеры-и-ошибки)
8. [Дополнительные методы](#дополнительные-методы)

---

## Управление отображением

### ПоказатьНомераСтрок()
Показать номера строк в редакторе.

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.ПоказатьНомераСтрок(ЭтотОбъект);
```

**JavaScript метод:** `showLineNumbers()`

---

### СкрытьНомераСтрок()
Скрыть номера строк в редакторе.

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.СкрытьНомераСтрок(ЭтотОбъект);
```

**JavaScript метод:** `hideLineNumbers()`

---

### Миникарта(Показать)
Показать или скрыть миникарту кода.

**Параметры:**
- `Показать` (Булево) - Истина для показа, Ложь для скрытия

**Пример использования:**
```bsl
// Показать миникарту
конс_ОбщийМодульКлиент.Миникарта(ЭтотОбъект, Истина);

// Скрыть миникарту
конс_ОбщийМодульКлиент.Миникарта(ЭтотОбъект, Ложь);
```

**JavaScript метод:** `minimap(enabled)`

---

## Работа с текстом

### ПолучитьТекст()
Получить весь текст из редактора.

**Возвращает:** Строка - текст редактора

**Пример использования:**
```bsl
ТекстКода = конс_ОбщийМодульКлиент.ПолучитьТекст(ЭтотОбъект);
Сообщить("Длина кода: " + СтрДлина(ТекстКода));
```

**JavaScript метод:** `getText()`

---

### УстановитьТекст(Текст, Позиция, УчитыватьОтступПервойСтроки)
Установить текст в редакторе.

**Параметры:**
- `Текст` (Строка) - текст для установки
- `Позиция` (Неопределено) - позиция вставки (необязательно)
- `УчитыватьОтступПервойСтроки` (Булево) - учитывать отступ

**Пример использования:**
```bsl
НовыйКод = "Процедура Тест()
|	Сообщить(""Привет!"");
|КонецПроцедуры";

конс_ОбщийМодульКлиент.УстановитьТекст(ЭтотОбъект, НовыйКод, Неопределено, Истина);
```

**JavaScript метод:** `setText(text, range, usePadding)`

---

### ПолучитьВыделенныйТекст()
Получить выделенный текст.

**Возвращает:** Строка - выделенный текст

**Пример использования:**
```bsl
ВыделенныйТекст = конс_ОбщийМодульКлиент.ПолучитьВыделенныйТекст(ЭтотОбъект);
Если ЗначениеЗаполнено(ВыделенныйТекст) Тогда
	Сообщить("Выделено: " + ВыделенныйТекст);
КонецЕсли;
```

**JavaScript метод:** `getSelectedText()`

---

### ЗаменитьВыделенныйТекст(Текст)
Заменить выделенный текст на новый.

**Параметры:**
- `Текст` (Строка) - новый текст

**Пример использования:**
```bsl
// Заменить выделенный текст на "ЗАМЕНА"
конс_ОбщийМодульКлиент.ЗаменитьВыделенныйТекст(ЭтотОбъект, "ЗАМЕНА");
```

**JavaScript метод:** `selectedText(text)`

---

### ВставитьСтроку(НомерСтроки, Текст)
Вставить строку в указанную позицию.

**Параметры:**
- `НомерСтроки` (Число) - номер строки для вставки
- `Текст` (Строка) - текст для вставки

**Пример использования:**
```bsl
// Вставить комментарий в начало кода
конс_ОбщийМодульКлиент.ВставитьСтроку(ЭтотОбъект, 1, "// Автоматически сгенерированный код");
```

**JavaScript метод:** `insertLine(lineNumber, text)`

---

### ДобавитьСтроку(Текст)
Добавить строку в конец документа.

**Параметры:**
- `Текст` (Строка) - текст для добавления

**Пример использования:**
```bsl
// Добавить строку в конец
конс_ОбщийМодульКлиент.ДобавитьСтроку(ЭтотОбъект, "// Конец кода");
```

**JavaScript метод:** `addLine(text)`

---

### ОчиститьТекст()
Очистить весь текст в редакторе.

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.ОчиститьТекст(ЭтотОбъект);
```

**JavaScript метод:** `eraseText()`

---

## Навигация и поиск

### НайтиТекст(СтрокаПоиска)
Найти текст в редакторе.

**Параметры:**
- `СтрокаПоиска` (Строка) - текст для поиска

**Пример использования:**
```bsl
// Найти слово "Процедура"
конс_ОбщийМодульКлиент.НайтиТекст(ЭтотОбъект, "Процедура");
```

**JavaScript метод:** `findText(string)`

---

### ОткрытьВиджетПоиска()
Открыть виджет поиска (Ctrl+F).

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.ОткрытьВиджетПоиска(ЭтотОбъект);
```

**JavaScript метод:** `openSearchWidget()`

---

### ПерейтиКСтроке()
Открыть диалог перехода к строке (Ctrl+G).

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.ПерейтиКСтроке(ЭтотОбъект);
```

**JavaScript метод:** `gotoLine()`

---

### ПоказатьСтрокуВЦентре(НомерСтроки)
Показать указанную строку в центре экрана.

**Параметры:**
- `НомерСтроки` (Число) - номер строки

**Пример использования:**
```bsl
// Показать строку 50 в центре
конс_ОбщийМодульКлиент.ПоказатьСтрокуВЦентре(ЭтотОбъект, 50);
```

**JavaScript метод:** `revealLineInCenter(lineNumber)`

---

### ПерейтиКПарнойСкобке()
Перейти к парной скобке.

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.ПерейтиКПарнойСкобке(ЭтотОбъект);
```

**JavaScript метод:** `jumpToBracket()`

---

### ПолучитьТекущуюСтроку()
Получить номер текущей строки.

**Возвращает:** Число - номер строки

**Пример использования:**
```bsl
НомерСтроки = конс_ОбщийМодульКлиент.ПолучитьТекущуюСтроку(ЭтотОбъект);
Сообщить("Текущая строка: " + НомерСтроки);
```

**JavaScript метод:** `getCurrentLine()`

---

### ПолучитьТекущуюКолонку()
Получить номер текущей колонки.

**Возвращает:** Число - номер колонки

**Пример использования:**
```bsl
НомерКолонки = конс_ОбщийМодульКлиент.ПолучитьТекущуюКолонку(ЭтотОбъект);
Сообщить("Текущая колонка: " + НомерКолонки);
```

**JavaScript метод:** `getCurrentColumn()`

---

## Закладки

### ДобавитьЗакладку(НомерСтроки)
Добавить закладку на указанную строку.

**Параметры:**
- `НомерСтроки` (Число) - номер строки

**Возвращает:** Булево - успешность операции

**Пример использования:**
```bsl
// Добавить закладку на строку 10
Если конс_ОбщийМодульКлиент.ДобавитьЗакладку(ЭтотОбъект, 10) Тогда
	Сообщить("Закладка добавлена");
КонецЕсли;
```

**JavaScript метод:** `addBookmark(lineNumber)`

---

### УдалитьЗакладку(НомерСтроки)
Удалить закладку с указанной строки.

**Параметры:**
- `НомерСтроки` (Число) - номер строки

**Возвращает:** Булево - успешность операции

**Пример использования:**
```bsl
// Удалить закладку со строки 10
Если конс_ОбщийМодульКлиент.УдалитьЗакладку(ЭтотОбъект, 10) Тогда
	Сообщить("Закладка удалена");
КонецЕсли;
```

**JavaScript метод:** `removeBookmark(lineNumber)`

---

### ПолучитьЗакладки()
Получить список всех закладок.

**Возвращает:** Массив - номера строк с закладками

**Пример использования:**
```bsl
Закладки = конс_ОбщийМодульКлиент.ПолучитьЗакладки(ЭтотОбъект);
Для Каждого НомерСтроки Из Закладки Цикл
	Сообщить("Закладка на строке: " + НомерСтроки);
КонецЦикла;
```

**JavaScript метод:** `getBookmarks()`

---

## Координаты и выделение

### ПолучитьКоординатыВыделения()
Получить координаты текущего выделения.

**Возвращает:** Структура с полями:
- `startLineNumber` - начальная строка
- `startColumn` - начальная колонка
- `endLineNumber` - конечная строка
- `endColumn` - конечная колонка
- `isEmpty` - пустое ли выделение
- `selectedText` - выделенный текст

**Пример использования:**
```bsl
Координаты = конс_ОбщийМодульКлиент.ПолучитьКоординатыВыделения(ЭтотОбъект);
Сообщить("Выделение с " + Координаты.startLineNumber + ":" + Координаты.startColumn + 
         " по " + Координаты.endLineNumber + ":" + Координаты.endColumn);
```

**JavaScript метод:** `getSelectionCoordinates()`

---

### ПолучитьНомераСтрокВыделения()
Получить номера начальной и конечной строк выделения.

**Возвращает:** Структура с полями:
- `startLine` - начальная строка
- `endLine` - конечная строка
- `totalLines` - количество выделенных строк

**Пример использования:**
```bsl
Строки = конс_ОбщийМодульКлиент.ПолучитьНомераСтрокВыделения(ЭтотОбъект);
Сообщить("Выделено строк: " + Строки.totalLines);
```

**JavaScript метод:** `getSelectionLineNumbers()`

---

### ПоказатьКоординатыВыделения()
Показать панель с координатами выделения.

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.ПоказатьКоординатыВыделения(ЭтотОбъект);
```

**JavaScript метод:** `showSelectionCoordinates()`

---

### УстановитьВыделение(НачальнаяСтрока, НачальнаяКолонка, КонечнаяСтрока, КонечнаяКолонка)
Установить выделение по координатам.

**Параметры:**
- `НачальнаяСтрока` (Число) - начальная строка
- `НачальнаяКолонка` (Число) - начальная колонка
- `КонечнаяСтрока` (Число) - конечная строка
- `КонечнаяКолонка` (Число) - конечная колонка

**Пример использования:**
```bsl
// Выделить с 5:1 по 10:20
конс_ОбщийМодульКлиент.УстановитьВыделение(ЭтотОбъект, 5, 1, 10, 20);
```

**JavaScript метод:** `setSelection(startLine, startCol, endLine, endCol)`

---

## Темы оформления

### УстановитьТему(ИмяТемы)
Установить тему редактора.

**Параметры:**
- `ИмяТемы` (Строка) - имя темы

**Возможные варианты:**
- `bsl-white` - светлая тема
- `bsl-white-query` - светлая тема с подсветкой запросов
- `bsl-dark` - темная тема
- `bsl-dark-query` - темная тема с подсветкой запросов
- `bsl-edt-white` - светлая тема в стиле EDT
- `bsl-edt-dark` - темная тема в стиле EDT

**Пример использования:**
```bsl
// Установить темную тему
конс_ОбщийМодульКлиент.УстановитьТему(ЭтотОбъект, "bsl-dark");

// Установить светлую тему с подсветкой запросов
конс_ОбщийМодульКлиент.УстановитьТему(ЭтотОбъект, "bsl-white-query");
```

**JavaScript метод:** `setTheme(theme)`

---

### УстановитьРежимТолькоДляЧтения(ТолькоДляЧтения)
Включить или выключить режим только для чтения.

**Параметры:**
- `ТолькоДляЧтения` (Булево) - Истина для включения режима

**Пример использования:**
```bsl
// Включить режим только для чтения
конс_ОбщийМодульКлиент.УстановитьРежимТолькоДляЧтения(ЭтотОбъект, Истина);

// Выключить режим только для чтения
конс_ОбщийМодульКлиент.УстановитьРежимТолькоДляЧтения(ЭтотОбъект, Ложь);
```

**JavaScript метод:** `setReadOnly(readOnly)`

---

## Маркеры и ошибки

### УстановитьМаркеры(МаркерыJSON)
Установить маркеры ошибок/предупреждений в коде.

**Параметры:**
- `МаркерыJSON` (Строка) - JSON с массивом маркеров

**Формат маркера:**
```json
{
  "startLineNumber": 10,
  "endLineNumber": 10,
  "startColumn": 5,
  "endColumn": 15,
  "severity": "Error",
  "message": "Описание ошибки",
  "code": "ERR001",
  "source": "BSL Linter"
}
```

**Типы severity:**
- `Error` - ошибка (красный)
- `Warning` - предупреждение (желтый)
- `Info` - информация (синий)
- `Hint` - подсказка (серый)

**Пример использования:**
```bsl
МассивМаркеров = Новый Массив;

Маркер = Новый Структура;
Маркер.Вставить("startLineNumber", 10);
Маркер.Вставить("endLineNumber", 10);
Маркер.Вставить("startColumn", 1);
Маркер.Вставить("endColumn", 20);
Маркер.Вставить("severity", "Error");
Маркер.Вставить("message", "Переменная не объявлена");
Маркер.Вставить("code", "E001");
Маркер.Вставить("source", "Проверка кода");

МассивМаркеров.Добавить(Маркер);

ЗаписьJSON = Новый ЗаписьJSON;
ЗаписьJSON.УстановитьСтроку();
ЗаписатьJSON(ЗаписьJSON, МассивМаркеров);
МаркерыJSON = ЗаписьJSON.Закрыть();

конс_ОбщийМодульКлиент.УстановитьМаркеры(ЭтотОбъект, МаркерыJSON);
```

**JavaScript метод:** `setMarkers(markersJSON)`

---

## Дополнительные методы

### ОтформатироватьТекст()
Форматировать код в редакторе.

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.ОтформатироватьТекст(ЭтотОбъект);
```

**JavaScript метод:** `formatDocument()`

---

### ПереключитьРежимЗапроса(РежимЗапроса)
Переключить режим редактора (BSL/Запрос).

**Параметры:**
- `РежимЗапроса` (Булево) - Истина для режима запроса

**Пример использования:**
```bsl
// Включить режим запроса
конс_ОбщийМодульКлиент.ПереключитьРежимЗапроса(ЭтотОбъект, Истина);

// Вернуться в режим BSL
конс_ОбщийМодульКлиент.ПереключитьРежимЗапроса(ЭтотОбъект, Ложь);
```

**JavaScript метод:** `setLanguageMode(mode)`

---

### СброситьПозициюКнопкиПомощи()
Сбросить позицию кнопки справки к значению по умолчанию.

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.СброситьПозициюКнопкиПомощи(ЭтотОбъект);
```

**JavaScript метод:** `resetHelpButtonPosition()`

---

### ПоказатьСправку()
Показать справку программно.

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.ПоказатьСправку(ЭтотОбъект);
```

**JavaScript метод:** `showHelp()`

---

### СкрытьСправку()
Скрыть справку программно.

**Пример использования:**
```bsl
конс_ОбщийМодульКлиент.СкрытьСправку(ЭтотОбъект);
```

**JavaScript метод:** `hideHelp()`

---

## Примеры комплексного использования

### Пример 1: Работа с выделением
```bsl
// Получить выделенный текст
ВыделенныйТекст = конс_ОбщийМодульКлиент.ПолучитьВыделенныйТекст(ЭтотОбъект);

Если ЗначениеЗаполнено(ВыделенныйТекст) Тогда
	// Преобразовать в верхний регистр
	НовыйТекст = ВРег(ВыделенныйТекст);
	
	// Заменить выделенный текст
	конс_ОбщийМодульКлиент.ЗаменитьВыделенныйТекст(ЭтотОбъект, НовыйТекст);
	
	Сообщить("Текст преобразован в верхний регистр");
КонецЕсли;
```

### Пример 2: Навигация по коду
```bsl
// Получить текущую позицию
ТекущаяСтрока = конс_ОбщийМодульКлиент.ПолучитьТекущуюСтроку(ЭтотОбъект);
ТекущаяКолонка = конс_ОбщийМодульКлиент.ПолучитьТекущуюКолонку(ЭтотОбъект);

// Добавить закладку на текущую строку
конс_ОбщийМодульКлиент.ДобавитьЗакладку(ЭтотОбъект, ТекущаяСтрока);

// Перейти к строке 100
конс_ОбщийМодульКлиент.ПоказатьСтрокуВЦентре(ЭтотОбъект, 100);

// Получить список всех закладок
Закладки = конс_ОбщийМодульКлиент.ПолучитьЗакладки(ЭтотОбъект);
Сообщить("Всего закладок: " + Закладки.Количество());
```

### Пример 3: Установка маркеров ошибок
```bsl
// Создать массив маркеров
МассивМаркеров = Новый Массив;

// Добавить ошибку
Ошибка = Новый Структура;
Ошибка.Вставить("lineNumber", 15);
Ошибка.Вставить("startColumn", 1);
Ошибка.Вставить("endColumn", 50);
Ошибка.Вставить("severity", "Error");
Ошибка.Вставить("message", "Необъявленная переменная");
МассивМаркеров.Добавить(Ошибка);

// Добавить предупреждение
Предупреждение = Новый Структура;
Предупреждение.Вставить("lineNumber", 20);
Предупреждение.Вставить("startColumn", 1);
Предупреждение.Вставить("endColumn", 30);
Предупреждение.Вставить("severity", "Warning");
Предупреждение.Вставить("message", "Неиспользуемая переменная");
МассивМаркеров.Добавить(Предупреждение);

// Преобразовать в JSON
ЗаписьJSON = Новый ЗаписьJSON;
ЗаписьJSON.УстановитьСтроку();
ЗаписатьJSON(ЗаписьJSON, МассивМаркеров);
МаркерыJSON = ЗаписьJSON.Закрыть();

// Установить маркеры
конс_ОбщийМодульКлиент.УстановитьМаркеры(ЭтотОбъект, МаркерыJSON);
```

### Пример 4: Работа с темами
```bsl
// Переключение между темами
Процедура ПереключитьТему()
	
	ТекущаяТема = "bsl-white"; // Сохранить в реквизите формы
	
	Если ТекущаяТема = "bsl-white" Тогда
		НоваяТема = "bsl-dark";
	Иначе
		НоваяТема = "bsl-white";
	КонецЕсли;
	
	конс_ОбщийМодульКлиент.УстановитьТему(ЭтотОбъект, НоваяТема);
	ТекущаяТема = НоваяТема;
	
КонецПроцедуры
```

---

## Горячие клавиши

| Клавиши | Действие | Метод |
|---------|----------|-------|
| **Ctrl+F** | Поиск | ОткрытьВиджетПоиска() |
| **Ctrl+G** | Перейти к строке | ПерейтиКСтроке() |
| **Alt+Shift+F** | Форматировать | ОтформатироватьТекст() |
| **Ctrl+Shift+I** | Координаты выделения | ПоказатьКоординатыВыделения() |
| **Ctrl+[** или **Ctrl+]** | Перейти к скобке | ПерейтиКПарнойСкобке() |

---

## Заключение

Этот справочник содержит все основные методы для работы с консолью BSL. Все методы доступны через общий модуль `конс_ОбщийМодульКлиент` и принимают первым параметром форму (`ЭтотОбъект`).

Для получения дополнительной информации см.:
- `Monaco/ВОЗМОЖНОСТИ.md` - полное описание возможностей редактора
- `Monaco/README.md` - общая информация о редакторе
- `Monaco/ПОДСВЕТКА_ОШИБОК.md` - работа с подсветкой ошибок

**Версия документа:** 1.0  
**Дата:** 2024
